# Verify

- Package: `n8n-nodes-feedjolt`
- Credential: `feedjoltApi` (Bearer fjk_)
- Nodes: Feedjolt (declarative REST), FeedjoltTrigger (poll new posts)
- Base URL: `https://api.feedjolt.com/api/v1`
- Build: `npm install && npm run build && npm run lint`
- Dev: `npm run dev` → http://localhost:5678
- Do not npm publish until Marc says go (needs GH Actions provenance + trusted publisher)
